- in `complemented_lattice_instances.v`
  + `ltEprod` and `lt_pair` (originally from `preorder.v`) has been renamed to
    `ltEprod_pre` and `lt_pair_pre` to avoid the name conflict with the lemmas
    from `order.v` with the same names
    ([#1580](https://github.com/math-comp/math-comp/pull/1580),
    fixes [#1508](https://github.com/math-comp/math-comp/issues/1508)).
