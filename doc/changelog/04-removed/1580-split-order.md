- in `order.v`
  + the `DeprecatedSubOrder` module; the notations and `HB.instance` declared in
    it are moved to `porder.v` and `total_order.v`
    ([#1580](https://github.com/math-comp/math-comp/pull/1580)).
