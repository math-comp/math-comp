- in `algebra/algebraic_hierarchy/`
  + `ssralg.v`, use `nmodule.v`, `rings_modules_and_algebras.v`, `divalg.v`, and
    `decfield.v` instead. Alternatively, `algebraic_hierarchy.v` enables a bulk
    import, but the contents of exportation is not exactly the same as
    `ssralg.v`, cf. the "Added" section
    ([#1642](https://github.com/math-comp/math-comp/pull/1642),
    fixes [#1505](https://github.com/math-comp/math-comp/issues/1505)).
 - in `algebra/algebraic_hierarchy/`
  + `ssrnum.v`, use `orderedzmod.v`, `numdomain.v`, and `numfield.v` instead.
    Alternatively, `numeric_hierarchy.v` enables a bulk import, but the contents
    of exportation is not exactly the same as `ssrnum.v`, cf. the "Added"
    section
    ([#1642](https://github.com/math-comp/math-comp/pull/1642),
    fixes [#1505](https://github.com/math-comp/math-comp/issues/1505)).
